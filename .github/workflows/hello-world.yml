name: Hello World on Self-Hosted Runner

on:
  push:
    branches:
      - main

jobs:
  hello-world:
    runs-on: [self-hosted, on-premise]

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Print Hello World
      run: echo "Hello, World! This is running on a self-hosted, on-premise runner."
